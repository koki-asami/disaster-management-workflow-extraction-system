# frontend_eng（非推奨）

このディレクトリの **Mermaid 前提 UI** は現代化方針により **メインの `frontend/`（React Flow + FastAPI `/api/v1`）へ統合**しました。

- 新規開発・デプロイは **`frontend/`** を使用してください。
- 履歴参照のため残している場合は、ビルドパイプラインから外し、依存更新の対象にしないことを推奨します。
